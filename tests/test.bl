/*

Unified term: 'multi-value expression'.

CHECK:
Foo.{ x += 1 }
#if #if
x, y;
XX, YY: s32 = foo(); // type must be inferred


Single value:

a, b    := 10;
a, b, c := foo(); // foo returns single value

foo() catch {}
x :: foo();
x :: foo() catch {}

 */

#import "std/print"
#import "std/test"



Enum :: enum foo() { A; B; }

main :: fn () s32 {
	s: Enum;
	return 0;
}

foo :: fn () type #comptime {
	return s32;
}

